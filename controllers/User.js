function User(first_name,last_name,id,password,email,points,results,quizzes_list)
{
  	this.id = id,
    this.first_name = first_name,
    this.last_name = last_name,
    this.password = password,
    this.email = email,
    this.points = points,
    this.results = results,
    this.quizzes_list = quizzes_list
}

