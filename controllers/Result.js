class Result
{
  	constructor(id, user_id,quiz_id,date,num_correct_questions)
    {
        this.id = id,
        this.user_id = user_id,
        this.quiz_id = quiz_id,
        this.date = date,
        this.num_correct_questions = num_correct_questions
    }
  
}